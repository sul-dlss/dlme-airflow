metadata:
  version: 1
sources:
  arabic:
    description: "Items of Arabic origin"
    driver: iiif_json
    args:
      collection_url: https://www.loc.gov/collections/manuscripts-in-st-catherines-monastery-mount-sinai/?c=100&fo=json
      dtype:
    metadata:
      data_path: loc/st-catherines-monastery
      config: loc_st_catherines_monastery_config
      fields:
        id:
          path: "@id"
        description:
          path: "description"
        rendering:
          path: "rendering..@id"
        thumbnail:
          path: "thumbnail..@id"
